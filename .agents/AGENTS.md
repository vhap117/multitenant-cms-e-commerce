# Project Agent Rules

This file is used to define custom project-scoped rules, style guidelines, and behavioral constraints for agents working in this workspace.

Add your custom rules below:

- **Core Package Architecture:** When modifying or interacting with the `packages/my-vendor/core` package, you MUST first read `packages/my-vendor/core/ARCHITECTURE.md` to understand the domain entities, state machines, and primary workflows.
- **Proposal Documentation:** All architectural proposals, plans, and design documents MUST be saved directly into the `proposals/` directory at the root of the project.
